
    <view class="goods-item"  bindtap="navDetail">
   <image class="img" src="{{goodsitem.show.img}}"></image>
    <view class="desc-info">
         <view class="title">{{goodsitem.title}}</view>
         <view class="info">
             <view class="price">{{goodsitem.price}}</view>
             <view class="cafv">{{goodsitem.cfav}}</view>
         </view>
    
    </view>

</view>
